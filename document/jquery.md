```
$(document).ready(function () {

})
```

```
    $('#form-avatar').submit(function (e) {
        // 阻止表单的默认行为
        e.preventDefault()
    })
```

```
$(this).ajaxSubmit()
这个ajax插件来自jquery.form.min.js这个文件
```

```

在form表单里加上这个参数
enctype="multipart/form-data"
就是多媒体表单
```

